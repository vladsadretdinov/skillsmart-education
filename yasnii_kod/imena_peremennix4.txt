maxHeight -> heightMax
// максимальная длина высоты окна браузера

totalColumns -> columnsTotal
// результирующее количество колонок отображаемых в таблице

result -> filteredData
// переменная которая содержит результат выполнения функции

currentPage -> currentPageNumber
// текущая страница в пагинации

value -> bytesSize
// аргумент функции которая переводит количество байтов в удобно читаемый формат нужной размерности

sizeWord -> worldLength
// переменная в которой хранится длина слова дабы не вычелять длину в цикле несколько раз

value -> state
// аргумент функции которая рендерит блок в ui по состоянию обьекта

mobile -> isLayoutMobile
// переменная сервиса которая хранит состояние -включена ли мобильная раскладка или нет-

devicesNumber -> numDevices
// общее количество устройств

indexMaxLogLevel -> logLevelMaxIndex
// по этому индексу определяется максимальный уровень логирования

deviceState -> currentDeviceState
// хранит текущее состояние устройства
